// create board
CREATE TABLE BOARD (
  NO NUMBER(10) PRIMARY KEY NOT NULL,
  TITLE VARCHAR2(100) NOT NULL,
  CONTENTS VARCHAR2(4000) NOT NULL,
  LASTDATE date NOT NULL,
  WRITER VARCHAR2(50) NOT NULL,
  PASSWORD VARCHAR2(50) NOT NULL
);

// create sequence
CREATE SEQUENCE BOARD_SEQ;

// create trigger
CREATE OR REPLACE TRIGGER BOARD_TRG
BEFORE INSERT ON BOARD
FOR EACH ROW
BEGIN
  SELECT BOARD_SEQ.NEXTVAL
  INTO :NEW.NO
FROM DUAL;
END;

// insert data for test
INSERT INTO BOARD (TITLE, CONTENTS, LASTDATE, WRITER, PASSWORD) VALUES ('title', 'hello', TO_DATE('2016-06-20 07:22:49', 'YYYY-MM-DD HH24:MI:SS'), 'guest', '1234')
